sudo: required
language: php
  - 7.1
services:
  - docker

before_install:
  - docker build -t umutbariskarasar/CI-Practice -f docker/dockerfile-phpfpm .

before_script: 
  - composer install

script: phpunit